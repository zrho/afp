$case requestedRoute
  $of Just HomeR
    <p .header>
      <h1>_{MsgGameName}
      <h2>_{MsgCredits}
      <div .content>
        $maybe msg <- mmsg
           <script>
             setTimeout(function() {alert("#{msg}")}, 200);
        ^{widget}
  $of _
    $maybe msg <- mmsg
       <script>
         setTimeout(function() {alert("#{msg}")}, 200);
    ^{widget}
    <div .controlBox>
      <a .linkButton .blueButton href="@{HomeR}">_{MsgLinkHome}
